{
  "name": "shop-finance-system",
  "private": true,
  "scripts": {
    "start": "npm --prefix app run start",
    "start:web": "npm --prefix app run web",
    "start:server": "npm --prefix server run dev",
    "install:all": "npm --prefix server install && npm --prefix app install"
  }
}
